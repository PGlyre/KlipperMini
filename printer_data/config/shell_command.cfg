# ==================================================================================================
# === FILE: shell_command.cfg
# === PRINTER: Prusa MINI+ Klipper Mod
# === CLEAN VERSION: v1.1
# === NOTE: Shell commands and shell-backed macros only.
# ==================================================================================================


# ==================================================================================================
# === TEST COMMAND: HELLO_WORLD
# ==================================================================================================

[gcode_shell_command hello_world]
command: echo hello world
timeout: 2.0
verbose: True

[gcode_macro HELLO_WORLD]
description: Test shell command bridge
gcode:
    RUN_SHELL_COMMAND CMD=hello_world


# ==================================================================================================
# === KLIPPER BACKUP / GIT UPDATE
# === Usage:
# === update_git
# === update_git MESSAGE="your commit message"
# ==================================================================================================

[gcode_macro update_git]
description: Run Klipper-backup script with optional MESSAGE parameter
gcode:
    {% set message = params.MESSAGE|default() %}
    {% if message %}
        RUN_SHELL_COMMAND CMD=update_git_script_message PARAMS="'{params.MESSAGE}'"
    {% else %}
        RUN_SHELL_COMMAND CMD=update_git_script
    {% endif %}

[gcode_shell_command update_git_script]
command: bash -c "bash $HOME/klipper-backup/script.sh"
timeout: 90.0
verbose: True

[gcode_shell_command update_git_script_message]
command: bash -c "bash $HOME/klipper-backup/script.sh -c \"$0\""
timeout: 90.0
verbose: True
